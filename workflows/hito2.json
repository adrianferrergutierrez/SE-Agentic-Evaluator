{
  "version": "1.0",
  "name": "workflow_hito2",
  "description": "Evaluación de Hito 2: Memoria técnica, diagrama de clases del modelo de dominio y glosario de clases.",
  "metadata": {
    "rubric_id": "hito2",
    "created_at": "2026-06-10T17:41:30.985752",
    "version": "1.0"
  },
  "variables": {
    "input_docx": "path/to/document.docx",
    "input_rubric": "path/to/rubric.yaml",
    "output_dir": "path/to/output"
  },
  "steps": [
    {
      "id": "step_extract_docx",
      "tool": "docx_extract",
      "description": "Convertir documento DOCX a Markdown y extraer imágenes incrustadas.",
      "params": {
        "input": "${variables.input_docx}",
        "output_dir": "${variables.output_dir}/extracted"
      },
      "output": {
        "contents_md": "contents_md",
        "images": "images"
      },
      "on_error": "abort"
    },
    {
      "id": "step_describe_diagrams",
      "tool": "describe_diagrams",
      "description": "Generar descripciones textuales de diagramas e imágenes para habilitar evaluación multimodal.",
      "params": {
        "document_path": "${step_extract_docx.result.contents_md}",
        "model": "qwen3-vl-32b"
      },
      "output": {
        "updated_md": "updated_md",
        "descriptions_count": "descriptions_count"
      },
      "on_error": "skip"
    },
    {
      "id": "step_evaluate",
      "tool": "criterion_evaluator",
      "description": "Evaluar el documento procesado contra los criterios de la rúbrica sin análisis profundo de trazabilidad.",
      "params": {
        "document": "${step_describe_diagrams.result.updated_md}",
        "config": "${variables.input_rubric}",
        "full": false,
        "output_dir": "${variables.output_dir}/evaluation"
      },
      "output": {
        "scores": "scores",
        "output_dir": "output_dir"
      },
      "on_error": "abort"
    },
    {
      "id": "step_grade",
      "tool": "grader",
      "description": "Calcular la calificación final ponderada según los pesos de la rúbrica.",
      "params": {
        "scores": "${step_evaluate.result.scores}",
        "config": "${variables.input_rubric}"
      },
      "output": {
        "weighted_final": "weighted_final",
        "mean_xbar": "mean_xbar",
        "performance_level": "performance_level"
      },
      "on_error": "abort"
    },
    {
      "id": "step_report",
      "tool": "report_generator",
      "description": "Generar el informe final consolidado con puntuaciones y retroalimentación.",
      "params": {
        "document": "${step_describe_diagrams.result.updated_md}",
        "eval_dir": "${step_evaluate.result.output_dir}",
        "config": "${variables.input_rubric}",
        "scores": "${step_evaluate.result.scores}",
        "output": "${variables.output_dir}/report.md"
      },
      "output": {
        "report_path": "report_path"
      },
      "on_error": "abort"
    }
  ]
}